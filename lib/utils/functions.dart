import 'dart:math';

class Functions {
  static int randomUid() {
    Random random = Random();
    return random.nextInt(1000000000000000000);
  }

  static saveFavouriteQuoteIds(String id) {
    // code to save the favourite quotes id...
    print(id + ' is pressed');
  }

  

  // static indexFinderForQuote(dynamic data, dynamic obj) {
  //   if (obj['description']=== )
  // }
}
